meta:
  file_pattern: "simple_test"

attributes:
  title: "Test Product"

dimensions:
  - name: time
    size: null

variables:
  - meta:
      name: time
      datatype: <float64>
      required: true
    dimensions:
      - time
    attributes:
      units: "seconds since 1970-01-01"
